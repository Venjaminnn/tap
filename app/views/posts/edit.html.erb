<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
     <style>
      #notice {
        display: none; /* скрыть элемент по умолчанию */
        position: fixed;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
        z-index: 9999;
        width: 300px;
        padding: 10px;
        background-color: #fff;
        box-shadow: 0px 5px 10px rgba(0,0,0,0.3);
        border: 1px solid #ccc;
      }

      #notice.show {
        display: block; /* показать элемент, если у него есть класс .show */
        animation: fade 1s ease-out forwards;
      }
    </style>
    <script>
      const notice = document.querySelector('#notice');

      notice.classList.add('show');
      setTimeout(() => {
        notice.classList.remove('show');
      }, 5000);
    </script>
  </head>
  
    <body class="edit">
    <section class="form-edit">
      <p id="notice"><%= flash[:notice] %></p>
        <div class="editing form">
          <header>Edit post</header>
          <%= form_with url: post_path(@post), method: :patch do |f| %>
            <td class="caption"><%= f.text_field :comment, placeholder: "Write a caption..." %><br></td>
            <td class="button"><%= f.submit "Save"%></td>
          <% end %>
        </div>
      </section>
    </body>
</html>